name: timetogo
description: Estimation de temps en montagne — ski de rando, randonnée alpine, trail

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^7.0.2
  flutter_map_tile_caching: ^9.1.0
  latlong2: ^0.9.0
  geolocator: ^11.0.0
  provider: ^6.1.0
  http: ^1.2.0
  path_provider: ^2.1.0
  archive: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
